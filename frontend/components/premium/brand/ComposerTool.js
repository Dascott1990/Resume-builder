"use client";
// ComposerTool.js — placeholder shell for the workspace's post composer,
// filled in fully at step 6 (this file is created early, in step 3, so
// the page shell has something real to mount and switch zones with).
export function ComposerTool({ token }) {
  return <p className="m-0 text-[12.5px] text-muted-foreground">Post composer — coming up next.</p>;
}
