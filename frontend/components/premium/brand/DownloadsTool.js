"use client";
// DownloadsTool.js — placeholder shell for logo/asset download & share,
// filled in fully at step 5 (this file is created early, in step 3, so
// the page shell has something real to mount and switch zones with).
export function DownloadsTool({ token }) {
  return <p className="m-0 text-[12.5px] text-muted-foreground">Download & share — coming up next.</p>;
}
