"use client";
// SchedulerTool.js — placeholder shell for the workspace-scoped
// scheduler, filled in fully at step 9 (this file is created early, in
// step 3, so the page shell has something real to mount and switch
// zones with).
export function SchedulerTool({ token, workspace }) {
  return <p className="m-0 text-[12.5px] text-muted-foreground">Scheduler — coming up next.</p>;
}
